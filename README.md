# N9TTA — сайт-портфолио

Одностраничный сайт-портфолио арт-директора в стиле medieval occult revival.
Живёт на [n9tta.art](https://n9tta.art), деплоится на Vercel автоматически при пуше в `master`.

## Стек

- **Next.js 16** (App Router) + TypeScript
- **Tailwind CSS v4** — токены через `@theme` в `app/globals.css`
- **Framer Motion** — прелоадер, флип карточек, каскадное появление
- **GSAP + ScrollTrigger** — реакция на velocity скролла, дорисовка орнаментов
- **Lenis** — плавный скролл

Сборка не требует настроек в дашборде Vercel: фреймворк задан в `vercel.json`.

## Запуск

```bash
npm install
npm run dev
```

Открыть http://localhost:3000. Продакшн-сборка — `npm run build`, линтер — `npm run lint`.

## Структура

```
app/
  layout.tsx        шрифты, metadata, глобальные слои (зерно, курсор, скролл), Nav, Footer
  page.tsx          Hero → Marquee → Works → About
  globals.css       дизайн-токены, утилиты, keyframes
  icon.svg          фавиконка
components/
  Hero, Marquee, Works, About, Footer, Nav, Preloader, SmoothScroll
  TarotCard         карточка кейса: 3D-tilt, флип, каскад
  CaseModal         модалка с полным текстом кейса
  RotatingRing      кольцо текста вокруг эмблемы
  ui/               курсор, magnetic, scramble, дорисовка орнаментов, зерно, кнопка-печать
  ornaments/        процедурные SVG: уголки, рамка, сигил, флёрон, эмблема
lib/
  cases.ts          контент кейсов
  site.ts           контакты, тексты «Обо мне», строки hero и футера
  motion.ts         media-query хуки: reduced motion, точный указатель, ширина
  gsap.ts           регистрация ScrollTrigger
public/assets/      обложки кейсов и портрет
```

## Как править контент

Админ-панели нет — контент лежит в коде.

- **Кейсы** — `lib/cases.ts`. Один объект на кейс: номер, название, категория, год, клиент, роль, услуги, `summary`, `challenge`, `approach`, `outcome`. Всё это выводится в модалке.
- **Контакты, «Обо мне», строки hero и футера** — `lib/site.ts`.
- **Обложки** — `public/assets/card-01…06`. Пропорция 3:4, ширина ~1200px, webp. Путь прописывается в поле `cover` у кейса.
- **Портрет** — `public/assets/portrait.svg`, пропорция 4:5.

Для `.svg`-обложек в `TarotCard` автоматически включается `unoptimized` — оптимизатор Next не обрабатывает SVG.

## Дизайн-токены

| Токен | Значение | Применение |
|---|---|---|
| `--color-parchment` | `#EFE6D0` | основной фон |
| `--color-ink` | `#16130E` | текст, линии, гравюры |
| `--color-blood` | `#8E1616` | акценты, hover, печати |
| `--color-gold` | `#B08D3E` | тонкие орнаменты |
| `--color-bone` | `#F5EFE0` | карточки, светлые поверхности |

Тёмной темы нет — только «бумажная».

### Шрифты

**Pirata One** (блэклеттер) не поддерживает кириллицу, поэтому используется только на латинице: вордмарк, `ART DIRECTOR`, навигация, бегущая строка. Все русские заголовки набраны антиквой **Cormorant Garamond**. Тексты — **IBM Plex Sans**, лейблы и нумерация — **IBM Plex Mono** (обе с кириллицей).

Если менять шрифты — проверяйте наличие кириллического subset, иначе русский текст провалится в системный fallback.

## Доступность и анимации

- При `prefers-reduced-motion` отключаются Lenis, GSAP, бегущая строка, вращение кольца и прелоадер.
- Кастомный курсор, 3D-tilt, magnetic и scramble включаются только при точном указателе и ширине от 768px (`lib/motion.ts`).
- Прелоадер гарантированно уходит по таймеру: в фоновой вкладке `requestAnimationFrame` не тикает, и без страховки он бы висел до фокуса вкладки.

## История

До августа 2026 здесь был статический сайт с админ-панелью, публиковавшей контент коммитами в GitHub через `api/publish.js`. Тот код и тексты (манифест, списки проектов брендинга и арт-дирекшна) остаются в git-истории — коммит `8a19ef9` и раньше.
